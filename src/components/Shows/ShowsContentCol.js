import React from 'react'

export const ShowsContentCol = ({children}) => {
    return (
        <div className="w-3/4 lg:w-1/2 flex flex-col justify-center">
            {children}
        </div>
    )
}
